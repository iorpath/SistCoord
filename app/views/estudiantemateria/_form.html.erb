<%= form_for(@estudiantematerium) do |f| %>
  <% if @estudiantematerium.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@estudiantematerium.errors.count, "error") %> prohibited this estudiantematerium from being saved:</h2>

      <ul>
      <% @estudiantematerium.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
